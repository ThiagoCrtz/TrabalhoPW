@model IEnumerable<WebApplication1.Models.Evento>

@{
    ViewBag.Title = "List";
}

<h2>List</h2>

<p>
    @Html.ActionLink("Create New", "Create")
</p>
<table class="table">
    <tr>
        <th>
            @Html.DisplayNameFor(model => model.Local)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.Data)
        </th>
        <th></th>
    </tr>

@foreach (var item in Model) {
    <tr>
        <td>
            @Html.DisplayFor(modelItem => item.Local)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.Data)
        </td>
        <td>
            @Html.ActionLink("Edit", "Edit", new { id = Model.ToList<WebApplication1.Models.Evento>().IndexOf(item) }) |
            @Html.ActionLink("Details", "Details", new { id = Model.ToList<WebApplication1.Models.Evento>().IndexOf(item) }) |
            @Html.ActionLink("Delete", "Delete", new { id = Model.ToList<WebApplication1.Models.Evento>().IndexOf(item) })
        </td>
    </tr>
}

</table>
