@model WebApplication1.Models.Celular

@{
    ViewBag.Title = "Excluir Carro";
}

<div class="container mt-4">
    <div class="card shadow-lg">
        <div class="card-header text-center bg-danger text-white">
            <h2>Excluir celulara</h2>
        </div>
        <div class="card-body d-flex flex-column align-items-center">
            <h3 class="text-center text-danger">Tem certeza que deseja excluir este celular?</h3>
            <hr class="w-100" />
            <dl class="row w-75">
                <dt class="col-md-5 text-md-end fw-bold">
                    @Html.DisplayNameFor(model => model.Numero)
                </dt>
                <dd class="col-md-7 text-md-start">
                    @Html.DisplayFor(model => model.Numero)
                </dd>

                <dt class="col-md-5 text-md-end fw-bold">
                    @Html.DisplayNameFor(model => model.Marca)
                </dt>
                <dd class="col-md-7 text-start">
                    @Html.DisplayFor(model => model.Marca)
                </dd>

                <dt class="col-md-5 text-md-end fw-bold">
                    @Html.DisplayNameFor(model => model.Novo)
                </dt>
                <dd class="col-md-7 text-md-start">
                    @Html.DisplayFor(model => model.Novo)
                </dd>
            </dl>

            @using (Html.BeginForm())
            {
                @Html.AntiForgeryToken()
                <div class="mt-3 d-flex gap-3">
                    <input type="submit" value="Excluir" class="btn btn-danger btn-lg px-4" />
                    @Html.ActionLink("Voltar para a Lista", "Lista", null, new { @class = "btn btn-secondary btn-lg px-4" })
                </div>
            }
        </div>
    </div>
</div>
